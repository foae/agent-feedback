# Moved

The API contract lives at [`docs/api.md`](api.md). This file remains as a
redirect for links written against API 1.0; every 1.0 request documented here
before is still valid and described there.
